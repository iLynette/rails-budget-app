<div class="wblock">
  <div class="block">
    <%= form_with(model: category) do |form| %>
      <% if category.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

          <ul>
            <% category.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="ame">
        <%= form.text_field :name, class: 'catn', placeholder: 'Enter Category name' %>
      </div>
      <hr />
      <div class="ame">
        <%= form.text_field :icon, class: 'caticon', placeholder: 'Enter Icon Name' %>
      </div>

      <div class="sub">
        <%= form.submit 'Add', class: 'submits' %>
      </div>
    <% end %>
  </div>
</div>
